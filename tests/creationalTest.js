test( "Abstract test", function Toyota(model) {
	ok( this.model = model, "Toyota created!" );
	ok( this.model = model, "Lexus created!" );
});

test( "Builder test", function durgLab() {
	ok( this.model = model, "Lexus created!" );
});
